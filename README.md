# movie_recommendation_system
A content-based movie recommender that reads a movie's plot description, turns it into a "meaning vector" using an Embedding + Attention neural network, and recommends similar movies using cosine similarity — no user history required
